import { AppStateActionKeyTypes, AppStateActionType } from '../types'

export const ACTION_TYPES: AppStateActionType = {
  // SET_ASDF: 'SET_ASDF
}

export const ACTION_KEYS: AppStateActionKeyTypes = {
  // ASDF: 'asdf
}
